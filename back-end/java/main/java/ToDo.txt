1.) Add Buxton, Chen, Danoff, Sternlight?, Wayland? to dorm building and campus locations

3.) Replace the int for floorNumbers with its own enum


5.) Update getRooms in RoomParser to be a list instead of an iterator to make the code more readable

6.) Make it so API can take 1 or one for roomCapacity.
