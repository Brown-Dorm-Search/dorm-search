1.) Add Buxton, Chen, Danoff, Sternlight?, Wayland? to dorm building and campus locations

2.) Use Jackson's Parser instead of initializes the main class with an empty dorm room list.

3.) Replace the int for floorNumbers with its own enum

4.) Change DormBuilding class to an enum of dormBuilding names, and a record that maps the dormBuildingNames
to the dormBuildings.